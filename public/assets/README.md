# Assets Folder

Store your custom images, videos, and other media assets here.
You can reference them in your components or `constants.tsx` file (e.g., `/assets/my-image.jpg` if using a build tool that handles public assets, or import them directly in React).